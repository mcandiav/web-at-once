{
  "name": "web-at-once",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync:media": "node scripts/ingest-media.mjs && node scripts/sync-media.mjs",
    "dev": "npm run sync:media && next dev",
    "build": "npm run sync:media && next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.2.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.8.2"
  }
}
